# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# Environment variables (보안 정보 - Git에 업로드 금지)
.env
.env.local
.env.*.local

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Android 환경 변수 설정 파일 (빌드 시 자동 생성됨 - Git에 업로드 금지)
/android/local.properties

# iOS/macOS build artifacts
/ios/build/
/macos/build/
/macos/DerivedData/

# iOS 환경 변수 설정 파일 (빌드 시 자동 생성됨 - Git에 업로드 금지)
/ios/Flutter/GoogleEnv.xcconfig

# Firebase 설정 파일
# 주의: 현재는 더미 템플릿 파일이므로 커밋 가능
# 실제 Firebase 키는 .env 파일에서 관리하며, .env는 .gitignore에 포함됨

# Claude Code (프로젝트 설정 파일과 캐시)
.claude/
.claude-plugins/
.claudedocs/
CLAUDE.md